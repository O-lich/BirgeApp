class DiaryModel {
  final String title;
  final String subtitle;

  DiaryModel({
    required this.title,
    required this.subtitle,
  });

  DiaryModel.create({
    required this.title,
    required this.subtitle,
  });
}